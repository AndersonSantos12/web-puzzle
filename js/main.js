/* ============================================================
   ARQUIVO PRINCIPAL - Inicialização da Aplicação
   ============================================================ */

// Cria instância da UI quando o documento estiver completamente carregado
// Todos os scripts devem estar carregados antes disso
document.addEventListener("DOMContentLoaded", () => {
  new WordSearchUI();
});
